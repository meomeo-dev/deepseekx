---
name: we-feature-dev
description: Use when starting or continuing GitHub Flow feature development
  in __WE_PROJECT_NAME__ with branch preflight, scoped checks, and PR hygiene.
---

# we-feature-dev

用于本仓库日常功能开发（feature development）、修复、重构、
实验命令维护和项目本地 workflow 变更。目标是在 LLM 会话无状态时，
仍然先管好分支、工作区和提交边界。

## 范围

- 从 `main` 创建或继续 `feature/<slug>` 分支。
- 按 GitHub Flow 准备变更，最终面向 `main` 合并。
- 支持本仓库的二次集成开发：私有仓库是开发事实来源。
- 支持普通代码、测试、文档、实验模板和 WE skill 变更。
- 需求边界不清时，先使用 `$we-requirement-intake`。
- 高风险或复杂方案改动前，先使用 `$we-design-note`。
- 不做 npm 发布；发布使用 `$we-publish`。
- 不做私有/公开双仓库同步；发布镜像使用 `$we-release`。

## 分支模型

- 私有工作区：`__WE_PRIVATE_ROOT__`。
- `origin` 必须指向私有 GitHub：
  `__WE_PRIVATE_REMOTE_URL__`。
- 主干（trunk）：`main`。
- 开发分支：`feature/<slug>`。
- 公开仓库 `__WE_PUBLIC_ROOT__` 不在本技能中修改。
- npm metadata 仍指向公开 GitHub，这是发布语义，不是开发远端。
- 默认按单人 GitHub Flow 执行：feature 分支完成后本地
  fast-forward 合并回 `main`，PR 是可选审查步骤，不是必需步骤。
- 如果用户要求专业团队流程、展示流程、多人协作或发布前审查，
  切换为 professional mode：PR、远端 CI 和受保护 main 语义为必需。

## 硬入口门禁

开始读代码实现或编辑文件前，先运行：

```bash
.agents/skills/we-feature-dev/scripts/preflight_feature_dev.sh
```

检查并向用户说明：

- 当前分支。
- remote URL 是否符合私有仓库。
- tracked dirty files。
- untracked files。
- secret-like untracked paths。
- 当前分支相对 `origin/main` 的 ahead/behind。
- 本地 `main` 相对当前分支的关系。
- npm package 关键事实。

如果只有安全的 skill 草稿或用户已明确的工作文件未跟踪，可以继续，
但必须说明不会误 stage 无关文件。

## 分支规则

- 不在 `main` 上直接做普通 feature 开发。
- 如果当前在 `main`，先创建 `feature/<slug>`。
- 如果当前在 `feature/<slug>`，preflight 通过后继续。
- 如果当前在非 `main` 且非 `feature/*` 分支，停止并说明风险。
- 有 tracked dirty files 时，不切换分支，除非用户明确批准。
- 不自动 stash、reset、clean、drop、force push 或覆盖用户工作。
- slug 使用小写 kebab-case，来自需求核心名词。

从 `main` 创建分支：

```bash
git switch main
git pull --ff-only origin main
git switch -c feature/<slug>
```

如果当前 feature 分支已经适合当前需求，继续使用，不重复建分支。

## 单人 GitHub Flow

默认流程：

1. 从干净且最新的 `main` 创建 `feature/<slug>`。
2. 在 feature 分支完成一个清晰需求单元。
3. 运行相关检查并提交。
4. 工作区干净后，把 feature 分支 fast-forward 合并回 `main`。
5. 从更新后的 `main` 创建下一条 feature 分支。

本地合并命令：

```bash
git switch main
git merge --ff-only feature/<slug>
git switch -c feature/<next-slug>
```

如果本地 `main` 落后远端，先执行：

```bash
git pull --ff-only origin main
```

PR 只在用户明确要求审查记录、远端 CI、发布前协作确认，或需要 GitHub
合并记录时创建。单人日常开发不强制 PR。

不要在已完成并已提交的 feature 分支继续塞入不同性质的新需求。
遇到下一个独立问题时，先合并已完成分支，再从 `main` 开新分支。

## Professional Mode

以下任一条件成立时，使用 professional mode：

- 用户明确要求专业流程、PR、review、团队协作或展示给他人。
- 改动影响公开 CLI 行为、npm 包内容、CI、release、tag 或公开仓库。
- 改动风险高，直接合并到 `main` 会难以解释或回滚。

professional mode 规则：

1. 先使用 `$we-branch-protection` 检查 `main` 是否有远端硬门禁。
2. feature 分支必须推送到 `origin feature/<slug>`。
3. 必须创建 PR，PR 描述包含范围、风险、检查命令和跳过项。
4. PR CI 必须通过；需要 Windows 时使用 `$we-ci-maintenance` 手动触发。
5. 不做本地直接 fast-forward 合并，除非用户明确回到 solo mode。
6. 合并前确认 `main` 没有落后远端，且 release 相关变更已解释。

solo mode 仍适合低风险个人开发；professional mode 适合需要审查记录、
远端门禁和可展示流程的场景。

## 开发流程

1. 若需求边界不清，先用 `$we-requirement-intake` 明确验收标准。
2. 若改动高风险或方案复杂，先用 `$we-design-note` 记录决策。
3. 运行 preflight，并判断是否需要新建 feature 分支。
4. professional mode 先用 `$we-branch-protection` 检查远端硬门禁。
5. 阅读 `AGENTS.md`、相关代码、测试和本地 skill 约束。
6. 用最小范围修改满足需求，遵守现有架构和命名。
7. 新增或更新聚焦测试；高风险改动扩大测试范围。
8. 优先运行增量检查；提交或 PR 前运行必要完整门禁。
9. 只 stage 相关文件。
10. 用户要求提交时，用清晰提交信息提交。
11. 用户要求合并或当前需求完成时，solo mode 优先本地合并回
   `main`。
12. professional mode 或用户要求 PR 时，推送到 `origin feature/<slug>`
    并创建 PR。

## 检查策略

日常开发优先：

```bash
npm run test:changed
npm run typecheck
```

提交前按改动范围补充：

```bash
npm run lint
npm run test:all
npm run package:verify
```

发布前或公共行为大改动使用：

```bash
npm run release:preflight
```

live API tests 默认不跑。只有用户要求，或改动影响 provider runtime
且需要真实上游验证时，运行相关 live test，并说明网络风险。

## Skill 变更检查

只改 WE skill 时，运行相关检查：

```bash
target=.agents/skills/we-<short-name>
bash -n "$target"/scripts/*.sh
python3 - <<'PY'
from pathlib import Path
root = Path('.agents/skills/we-<short-name>')
for path in root.rglob('*'):
    if not path.is_file():
        continue
    for i, line in enumerate(path.read_text(errors='ignore').splitlines(), 1):
        if len(line) > 88:
            print(f'{path}:{i}:{len(line)}')
PY
```

没有脚本时跳过 `bash -n`。只检查本次改动的 skill 目录。

## 提交卫生

提交前运行：

```bash
git status --short
git diff --cached --name-only
```

确认：

- 没有 `.env`、key、token、credential、cache、tarball 或临时目录。
- 没有 `_tasks/`、`_workflows/` 私有工作流被误纳入公开发布路径。
- 没有无关用户改动被 stage。
- generated files 是有意产物。
- npm 包文件白名单由 `package.json` 和 `package-verify` 控制。

## PR 指引

PR base 使用 `main`，head 使用 `feature/<slug>`：

```bash
git push -u origin feature/<slug>
gh pr create --base main --head feature/<slug>
```

PR 描述包含：

- 改动范围。
- 关键行为变化。
- 检查命令和结果。
- 跳过的检查及原因。
- 是否影响 npm 包内容或发布流程。

## 最终答复

报告：

- 当前分支。
- preflight 是否通过。
- 是否新建或继续 feature 分支。
- 变更文件。
- 检查命令和结果。
- commit hash，如果已提交。
- push 或 PR URL，如果已创建。
- 保留的 dirty 或 untracked files。
